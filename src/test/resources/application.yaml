logging:
  level:
    root: DEBUG
server:
  error:
    whitelabel:
      enabled: false
  port: 8882
spring:
  main:
    lazy-initialization: true
  application:
    name: randomizer
  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: false